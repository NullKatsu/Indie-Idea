using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ingredient : MonoBehaviour
{
    // Gameobject
    //Rigid BOdy
    // Start is called before the first frame update
    void Start()
    {
        // holds multiple array[,] GamePiece 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
